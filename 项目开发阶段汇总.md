# 雅洁五金2026年工程图册 — 开发阶段汇总

**整理说明**：汇总自历史对话（HTML 对齐「阶段一」执行记录）与仓库内 `plans/` 文档。  
**日期**：2026-03-18

---

## 一、与「最新页面代码.html」对齐 — 已执行的「阶段一」（对话/落地项）

**背景**：程序内页面插入与 HTML 中 PAGE 01～13 对应；先做高优先级三项对齐。

| 序号 | 内容 | 涉及文件/说明 | 对话结论 |
|------|------|----------------|----------|
| 1 | 打印 / PDF 版式收窄 | `@media print` 下 `.a4-page` 不再与 `.page-content` 双重 padding | ✅ 已完成 |
| 2 | 战略合作伙伴 3×5 | `pageTemplates.js` 中 `partners` 为 **15 条** | ✅ 已完成 |
| 3 | 目录页随图册变化 | `TableOfContentsPage.vue` 基于 **`useCatalogStore().pages`** 计算页码 | ✅ 已完成 |

**自测要点**：打印预览 15mm 边距、插入战略合作伙伴见 15 格、调整页面顺序后目录页码更新。

---

## 一点五、阶段二（内容页与 HTML 对齐）— 已落地（2026-03-18）

| 页面 | 说明 |
|------|------|
| **工程案例** `ProjectCasesPage.vue` | 改为 **3×3** 网格；图区 **60mm**、标题/英文；页眉固定为「经典工程案例」；按最新验收**取消左上角分类角标**。 |
| **表面处理** `SurfaceFinishesPage.vue` | 使用全局 **`grid-finishes` + `swatch-*`**（与 HTML PAGE06）；导语用模板 `intro`；移除错误的 `PageFooter` 引用。 |
| **公司简介** `CompanyIntroPage.vue` | 去掉金色描边故事卡，正文仅用 **`text-cols`** 双栏。 |
| **荣誉资质** `CertificatesPage.vue` | 页眉固定「荣誉与资质」；去掉内层重复 **`page-content`**；按最新验收改为 **2×3（每页 6 个）**以保证容器完整显示与图片更清晰。 |
| **战略合作伙伴** `PartnersPage.vue` | 页眉固定「战略合作伙伴」；格内以**文案居中**为主，Logo 可选角标上传。 |
| **模板** `pageTemplates.js` | 合作伙伴 **15 条默认名称** 与 HTML 一致（万科/保利/华润…）。 |
| **封面 / 过渡 / 目录** | 未改大结构：封面维持现有画册封面；过渡页、目录已与规范接近（目录阶段一已 store 派生）。 |

### 阶段二补充（按最新验收与能力增强）

- **阶段二验收口径**：按当前需求验收，阶段二视为**已完成**；其中封面未严格复刻 HTML，资质页网格按可读性做了调整（见上表）。
- **全局图片能力增强（适用于内容页与产品页）**：
  - **删除图片**：`ImageUploader.vue` 支持删除（`update:src(null)` 清空）。
  - **图片编辑**：支持缩放 / 旋转 / 透明度；并增加 **适配模式**（`contain`/`cover`/`fill` 循环）。
  - **覆盖范围**：公司简介 hero、资质证书、工程案例、合作伙伴 logo、自由编辑页图片块、以及产品卡片（实拍图与线图）。
- **统一图片容器底色**：
  - 在 `design-tokens.css` 增加 `--color-image-bg`，并在各页面/组件的图片容器与占位中统一引用。

---

## 二、`plans/最新页面代码实施计划.md` — 四阶段总路线（HTML 规范）

### 阶段一：基础设计系统与核心组件（2–3 天）
- 更新 `design-tokens.css` 与最新规范一致  
- 优化 `A4Page.vue`（15mm 边距）  
- 重构 `ProductCard.vue`（如 48mm 高度等）  
- 统一网格工具类  

### 阶段二：内容页面实现（3–4 天）
- 封面、公司简介、荣誉资质(4×2)、战略伙伴(3×5)、工程案例(3×3)、表面处理(6×6)、目录、过渡页等 8 类内容页对齐  

### 阶段三：产品页面实现（2–3 天）
- 实拍产品页、复合产品页、产品卡片一致、封底  

### 阶段四：集成测试与优化（1–2 天）
- 跨浏览器、打印/PDF、响应式、性能与代码清理  

---

## 三、`plans/控制组件与页面映射实施计划.md` — 三阶段（控制与映射）

| 阶段 | 周期 | 内容 |
|------|------|------|
| 阶段一 | 约 1 天 | 清理和优化控制组件与页面映射 |
| 阶段二 | 10–12 天 | 逐页落实设计规范 |
| 阶段三 | 2–3 天 | 集成测试与优化 |

（详细任务见同目录 `控制组件与页面映射实施进度.md`。）

---

## 四、其它文档中的阶段划分（索引）

- **`基于页面代码txt的修复计划.md`**：阶段 A～E（基础架构 → 产品 → 特殊页 → 内容页 → 验证清理）  
- **`目录分析报告与修复计划.md`** / **`雅洁五金2026图册全面分析报告.md`**：阶段一至三（映射/逻辑 → 视觉 → 优化测试）  
- **`component-split-plan.md`** / **`ui-reconstruction-plan.md`**：组件拆分与 UI 重构的分阶段迁移  

---

## 五、与《项目进度报告》的关系

`项目进度报告.md` 按**功能模块**（PDF、封面、环境素材、下一步建议）叙述，**未使用「阶段一/二」编号**；与上表「阶段」是不同维度的归纳，可对照使用。

---

*本文档为人工整理汇总，具体任务以各 `plans/*.md` 原文为准。*
